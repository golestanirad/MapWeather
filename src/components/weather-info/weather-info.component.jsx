import React from "react";
// project files
import styles from "./weather-info.module.scss";

const WeatherInfo = () => {
    return<div className={styles.weatherInfo} > weather info ;) </div>
};

export default WeatherInfo;